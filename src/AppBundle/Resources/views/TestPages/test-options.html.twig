{% extends '::base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-lg-6 col-md-offset-3 col-md-offset-3 col-md-6 col-sm-12">
                {{ form_start(form) }}
                {{ form_row(form.book) }}
                {{ form_row(form.amount) }}
                <input type="submit" class="btn btn-1 btn-1f" value="Start the test!">
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}